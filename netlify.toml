[build.environment]
  NODE_VERSION = "14"
  NPM_FLAGS = "--version" # prevent Netlify npm install

[build]
  base = "."
  publish = "packages/site"
  command = "npx pnpm i --store=node_modules/.pnpm-store && pnpm run build && pnpm run gen
