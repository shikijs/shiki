// this needs to be a txt file, otherwise `jsr publish` fails while checking for slow types
if (true) {
// Y    Y R @colors
  
  let obj = {
    //      P @colors
    foo: 'foo'.split()(
    //              BBR @colors
  }];
  // @colors 2=P 3=R
  
let foo = ([[[[)]]]]
//        PRRRRPRRRR @colors
